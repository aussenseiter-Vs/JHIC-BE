services:
  api:
    build: .
    container_name: jhic-be
    ports:
      - "${HOST_PORT:-8085}:8080"
    environment:
      - PORT=8080
      - DATABASE_URL=${DATABASE_URL}
      - STORAGE_DIR=/app/data
    volumes:
      - jhic-data:/app/data
    restart: unless-stopped

volumes:
  jhic-data: